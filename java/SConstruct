# -*- python -*-
import glob, os.path

env = Environment()

dn = os.path.join('com', 'wl', 'www')
#env.Java(target = 'classes', source = glob.glob(os.path.join(dn, '*.java')))
env.Java(target = 'classes', source = 'sources')
